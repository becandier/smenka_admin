// Готовый промпт для ИИ (employee_tests/import-format.md, «Готовый промпт для нейросети»).
// Текст воспроизведён дословно — админ копирует его, подставляет тему/материал и вставляет
// готовый JSON в поле импорта (admin.md, «Кнопка «Импорт из JSON»»).
export const AI_TEST_IMPORT_PROMPT = `Ты — генератор тестов. Сгенерируй тест по теме/материалу ниже и верни
ТОЛЬКО валидный JSON (без markdown-обёртки, без комментариев, без текста
до и после), строго по этой схеме:

{
  "title": "краткое название теста",
  "description": "1–2 предложения инструкции или null",
  "pass_threshold_percent": 70,
  "max_attempts": 1,
  "reveal_answers": true,
  "shuffle_questions": false,
  "questions": [
    {
      "text": "формулировка вопроса",
      "type": "single_choice",         // или "multiple_choice"
      "points": 1,
      "options": [
        { "text": "вариант", "is_correct": true },
        { "text": "вариант", "is_correct": false }
      ]
    }
  ]
}

Требования:
- Язык вопросов и вариантов — русский.
- Количество вопросов: {N} (по умолчанию 10, если не указано).
- У single_choice ровно один вариант с is_correct: true.
- У multiple_choice хотя бы один вариант с is_correct: true.
- У каждого вопроса минимум 2, лучше 4 варианта.
- points ставь 1, если сложность не важна; более сложным вопросам — 2.
- Не добавляй никаких полей сверх схемы.

Тема / материал теста:
"""
{СЮДА ВСТАВИТЬ ТЕМУ ИЛИ ТЕКСТ МАТЕРИАЛА}
"""`;
